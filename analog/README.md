Stage D: POSTPONED indefinitely (decided 2026-09-16), not scheduled. See
`docs/00-spec.md`. Not a promise; if ever un-postponed it would be an
XGBoost-based surrogate-model exploration aid, never a claim that "AI
designed the circuit."
