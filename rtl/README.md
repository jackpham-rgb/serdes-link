# rtl/: Stage C (not started)

SystemVerilog reimplementation of `dfe.py` and `cdr.py`, cosimulated against
those Python modules as the golden model via cocotb. TODO once Stage A's
golden models (this repo) are stable. See the project howto (private
planning repo), section 3.
